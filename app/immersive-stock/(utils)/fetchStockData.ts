export default function fetchStockData(stockIds: string[]) {
    try {
        
    // const stockIds = ['6659'];
    const queries: {
        strategy1: string[];
        strategy2: string[];
      } = { strategy1: [], strategy2: [] };
    
      
      return queries;
    } catch (error) {
        console.log(error);
        
    }
}
